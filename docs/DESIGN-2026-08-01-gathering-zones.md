# 채집 영역 체제 설계 — 작업영역·서식지 자원화·숙식 움막

> **계획 상태:** 완료
> **상태 갱신:** 2026-08-02 — G1~G4 완료. 사냥터 비축 3배 상향, 구 저장 비축률 환산, 사냥막 영역 전체 고갈 경고·8일 쿨다운까지 후속 보정·검증함.

- 작성일: 2026-08-01 (사용자 구술 + Fable 검토·코드 대조)
- 관련 설계: [방어 개편](DESIGN-2026-08-01-defense-overhaul.md) — 숲 벨트 유지(벌목 통제)라는 지점에서 만나는 별도 트랙. 독립 착수 가능.

## 0. 문제 진단

1. 채집(벌목·사냥·약초·채광)이 전 맵 자유 방랑이라 **플레이어가 숲을 남길 수단이 없다** — 방벽림·경관·서식지 유지가 전부 운에 달렸다.
2. 채집 건물(벌목장·사냥막·약초막)이 하역처에 그쳐 **위치의 전략적 의미가 얕다.**
3. 사냥감·광물의 소진이 위치 선택으로 이어지지 않는다 — 옮겨 다닐 이유가 없다.
4. 채집 동선이 길어지면 하루 사이클(귀가·취침) 때문에 **밤늦게 돌아와 금방 나가는** 부자연스러운 왕복이 생긴다.

## 1. 확정 방향 (2026-08-01)

1. **작업영역 지정**: 벌목장·사냥막·약초막·채광장이 채광장의 반경 작업 모델(`mineWorkRadius`)처럼 **지정된 영역 안에서만** 채집한다. 영역은 건물을 선택해 위치·크기를 조절할 수 있다.
   - **공사터 개간 예외**: 신축·영역 확장·건물 이전 예정지의 나무는 작업영역과 무관하게 우선 처리한다. 이는 일반 자원 채집이 아니라 플레이어가 승인한 건설 선행 작업이며, 개간조 배정 → 공사터 벌목 → 일반 영역 벌목 순서를 불변식으로 둔다.
2. **배치 필수화**: 채집 직업은 해당 건물에 배정되어야 일한다 — 자유 방랑 채집 제거. 이에 따라 **채광장을 1티어(촌)로 내린다.**
   - 주의: R5에서 확정한 "노두 자유 채집 + 채광장 보 전용"을 **의도적으로 뒤집는** 결정이다. 당시엔 건물 없이 캐는 세계관에서 맞는 선택이었고, 새 체제에서는 채광장이 1티어 앵커가 된다. 노두 고정 배치(마을 옆 돌·철)는 그대로 유효 — 이제 "1티어 채광장의 첫 영역"이 된다.
3. **서식지 자원화**: 사냥터(서식지)에 천천히 회복되는 저장량을 둔다. 소진되면 옮겨 다니며 사냥하고, **주변 숲이 남아 있으면** 시간이 지나 회복된다. 벌목 통제(1)와 맞물려 "숲을 남기는 플레이"에 실질 보상이 생긴다.
4. **숙식 움막(임시 막)**: 작업장과 링크된 저렴한 움막을 지으면, 작업자가 마을에서 **식량·땔감을 가져가 움막에 두고 숙식하며** 일한다. 비축이 다하면 집으로 돌아가 하루 쉬고, 다음 날 다시 보급을 지고 나간다. 원거리 영역의 왕복 낭비(문제 4)를 해소하는 짝 메커니즘 — 영역 체제가 만든 원거리 작업지는 움막 없이는 비효율, 움막은 영역 없이는 무의미.

## 2. 단계 구성

| 단계 | 내용 | 비고 |
|---|---|---|
| G1 | ✅ **완료** — 벌목장 원형 작업영역의 중심 이동·반경 조절, 수동 배정 슬롯, 선택/배치 오버레이와 영역 내 숲·성목 요약. 배정 벌목꾼은 해당 영역만 벌목·하역 | 첫 판은 채광장과 같은 반경 문법으로 확정. 반경 3~10칸 |
| G2 | ✅ **완료** — 사냥막·약초막 이동·반경 조절 작업영역, 배정 작업자의 영역 제한·지정 거점 하역, 서식지 비축·사냥 소모·숲 조건부 일일 회복, 사냥막 비축 UI | 고갈 시 사냥 가능 타일이 닫히고 회복분이 생기면 다시 열린다. 구버전 서식지는 현재 숲 기준 최대 비축으로 복원 |
| G3 | ✅ **완료** — 배치 필수화 + 채광장 1티어 — 자유 방랑 제거, 시작 흐름·저장 마이그레이션·튜토리얼 연쇄 수정 | 벌목장은 3목재·2일로 완화. 공사터 개간은 미배정·영역 밖에서도 계속 최우선 |
| G4 | ✅ **완료** — 숙식 움막 — 작업장 자동 링크, 3일분 식량·땔감 직접 운반·국지 소모, 현지 취침, 소진 뒤 1일 귀가·재보급 루프 | 1작업장 1움막. 링크·재고·체류자·잔여 일수 UI와 배치/선택 오버레이 포함 |

### G1 구현 기록 (2026-08-02)

- `src/game/gatheringZones.ts`: 저장되는 영역 중심·반경의 조절, 원형 영역 판정과 답사한 숲·성목 요약을 단일 규칙으로 제공한다.
- `src/game/agents.ts`: 벌목장에 배정된 벌목꾼의 일반 벌목 목표와 하역처를 해당 벌목장으로 제한한다. 건설·확장·이전 예정지 개간은 그보다 먼저 배정되어 영역 밖에서도 처리한다. G1 당시 저장 호환용으로 남긴 미배정 자유 벌목은 G3에서 제거했다.
- `src/render/renderer.ts`, `src/components/SelectionContextBar.tsx`: 선택/배치 중 녹색 작업 반경을 그리고 영역 내 확인한 숲·벌목 가능한 성목 수와 중심 이동·반경 조절 UI를 표시한다.
- 검증: `node tools/game/test_lumber_camp_worksite.mjs`에서 영역 안 일반 벌목·영역 밖 일반 벌목 금지·영역 밖 공사터 우선 개간을 함께 고정. 작업자 슬롯·생산·저장·경로 결정성 표적 회귀, `npm run build` 통과.

### G2 구현 기록 (2026-08-02)

- `src/game/gatheringZones.ts`: 벌목장 전용 규칙을 사냥막·약초막까지 공유하는 채집 거점 작업영역으로 일반화했다. 세 거점 모두 독립 중심 이동과 반경 3~10칸 조절을 지원한다.
- `src/game/habitats.ts`, `src/game/simulation.ts`: 반경 안 숲 타일 수로 서식지 최대 비축을 계산하고, 사냥 1회마다 비축 1을 소모한다. 비축이 0이면 사냥터가 닫히며, 최소 숲이 남은 활성 서식지만 매일 최대치의 8%(최소 1.05)를 회복한다. 플레이테스트 후속으로 숲 타일당·최소·최대 비축과 최소 회복량을 모두 3배로 올렸다.
- `src/game/agents.ts`: 배정 사냥꾼·약초꾼은 지정 거점 영역 안에서만 목표를 고르고 해당 거점에 하역한다. G2 당시 남긴 미배정 자유 작업은 G3에서 제거했다.
- `src/components/SelectionContextBar.tsx`, `src/render/renderer.ts`: 사냥막·약초막 선택/배치 오버레이, 공용 영역 조절 UI, 사냥막 영역과 겹친 서식지 수와 현재/최대 비축을 표시한다.
- 저장 호환: 구버전 서식지의 누락된 비축 필드는 로드 시 현재 숲 기준 최대 비축으로 채운다.
- 밸런스 후속: 기존 저장의 현재/최대 비축 비율을 새 3배 최대치에 그대로 환산한다. 개별 서식지 하나가 비어도 같은 사냥막 영역에 사냥감이 남아 있으면 경고하지 않으며, 영역 전체 고갈 경고도 8일에 한 번으로 제한한다. 회복 알림은 최대치의 25%가 다시 찼을 때만 표시한다.
- 검증: `test_forest_habitats.mjs`, `test_gathering_zones_g2.mjs`, 작업자 슬롯·G1·기존 채집 생산·경로 결정성 회귀와 `npm run build` 통과.

### G3 구현 기록 (2026-08-02)

- `src/game/agents.ts`, `src/game/workerSlots.ts`: 벌목꾼·사냥꾼·약초꾼·채광꾼은 각각 벌목장·사냥막·약초막·채광장에 배정되어야 일반 채집을 한다. 채광도 배정 채광장 반경과 해당 거점 하역으로 고정했다. 다만 신축·확장·이전 공사터의 나무는 배정 여부와 작업영역을 건너뛰고 먼저 치운다.
- `src/game/buildings.ts`: 채광장의 보 등급 잠금을 풀어 촌 단계 건물로 내리고 비용·공기를 목재 6·석재 4·도구 1, 5일로 조정했다. 첫날 마찰은 시작 건물 지급 대신 벌목장을 목재 3·2일로 낮추는 방식으로 풀었다.
- `src/game/saveLoad.ts`: 구 저장의 방랑 채집꾼을 직업에 맞는 가장 가까운 완공 거점의 빈 슬롯에 자동 배정한다. 자리가 없으면 무직으로 전환하고 중요 안내 로그를 남긴다.
- `src/game/scenario.ts`, `src/components/TutorialCoach.tsx`, `src/game/tutorialStart.ts`, `src/game/guides.ts`: G3에서 벌목장·사냥막·약초막·채광장의 건설과 작업 슬롯 배정을 실제 목표·코치 동선에 반영했다. 후속 보정으로 길잡이 버전을 7로 올리고 첫 벌목장 터 직후 건축가를 먼저 배정한 뒤 벌목꾼·완공·작업 슬롯으로 잇는다. 마을 곁 돌·철 노두 보장은 첫 채광장 영역의 시작 자원으로 유지한다.
- 경로 실패 쿨다운은 게임 상태별 `WeakMap`으로 격리해 새 게임/불러오기에서 같은 주민 ID의 오래된 실패 기록이 새 경로 탐색을 막지 않게 했다.
- 검증: `test_gathering_assignment_g3.mjs`, `test_manual_orders.mjs`, `test_tutorial_scenario.mjs`, 광산·보 등급·자동 배정·채집 생산·G1~G2 회귀와 `npm run build` 통과.

### G4 구현 기록 (2026-08-02)

- `src/game/lodgingHuts.ts`, `src/game/buildings.ts`: 저비용 `숙식 움막`을 추가했다. 완공된 벌목장·사냥막·약초막·채광장의 작업영역 안에만 놓을 수 있고, 배치 시 가장 가까운 해당 거점에 자동 연결한다. 한 작업장에는 공사 중인 것까지 포함해 한 동만 허용한다.
- `src/game/agents.ts`: 연결 거점의 작업자가 마을 집에서 3일분 식량·땔감을 직접 꺼내 실제 짐으로 운반하고 움막 재고에 적재한다. 비축이 충분하면 저녁 여가·귀가 대신 움막으로 돌아가 자고, 소진 뒤 하루는 집으로 귀가해 쉰 다음 다음 날 다시 보급한다. 병자·격리자·건강 저하자는 현지 숙박에서 제외한다.
- `src/game/consumption.ts`, `src/game/simulation.ts`, `src/game/residents.ts`, `src/game/dayCycle.ts`: A안 국지 재고를 확정했다. 움막 체류자는 그날 마을 전역 소비에서 제외되고 움막 식량·땔감만 소비하며, 현지 취침은 일반 움집 수준의 실내 숙박으로 처리한다. 마을 주민의 기존 전역 소비는 바꾸지 않았다.
- `src/components/SelectionContextBar.tsx`, `src/render/renderer.ts`: 움막에서 연결 거점·체류 작업자·식량/땔감 잔여 일수를 보여 준다. 배치 미리보기와 움막/거점 선택 시 작업영역 및 황색 점선 링크를 그린다.
- `src/game/saveLoad.ts`: 구 저장과 비정상 저장에서 끊어진 링크·중복 링크·운반 중 상태를 정규화한다. 철거·공사 취소·이전에서도 연결 상태를 정리하거나 유효 범위 안으로 제한한다.
- 검증: `test_lodging_huts_g4.mjs`에서 영역 내 자동 링크·1거점 1움막·국지 소비·전역 소비 중복 방지·소진 귀가·실제 보급 운반·저장 정규화를 고정했다. `test_daily_cycle.mjs`에 현지 실내 취침과 현재 거점 배정 하역 회귀를 반영했고, G1~G3·물류·작업 명령·튜토리얼·채광장·빌드 서랍 회귀 및 `npm run build`를 통과했다.

## 3. 기존 시스템 접점 (2026-08-01 코드 대조)

| 신규 요소 | 재사용할 기존 문법 |
|---|---|
| 반경 작업영역 | 채광장 `mineWorkRadius` + `mineralDepositsInMineRange` |
| 영역 크기 조절 | 밭 드래그 크기 지정 (2026-07-17) — 사각형이면 그대로, 반경이면 선택 창 슬라이더 |
| 건물 배정 | 작업자 슬롯 (`buildingWorkerSlots`), `assignedBuildingForResident` |
| 채집 목표 탐색 | `gatherJob`/goal field — "전 맵 → 영역"으로 좁히는 구조 변경 (성능은 오히려 유리) |
| 서식지 | `habitats` (id·위치·반경·active) — 저장량·회복률 필드 추가 |
| 숲 재생 | `forestGrowth` — 서식지 회복의 "숲 잔존" 판정에 재사용 |
| 움막 취침 | 하루 사이클 위상(rest/toHome/sleeping) — 취침지를 집 대신 움막으로 |
| 움막 보급 | 건물 재고(`addBuildingStock`) + 운반 문법. 첫 판은 작업자 본인이 지고 간다(운반꾼 개입 없음 — 사용자 구술 그대로) |
| 겨울 난방 | 움막 체류 중 땔감 소모 — 기존 난방 소모 문법 |

## 4. G3의 파급 — 완료 기록

- [x] **시작 흐름**: 시작 벌목장을 주지 않고 벌목장 비용·공기를 목재 3·2일로 낮췄다. 터에 숲이 있으면 미배정 벌목꾼이 먼저 개간한다.
- [x] **튜토리얼 연쇄**: 벌목·사냥·약초·광물 단계에 거점 완공과 작업 슬롯 배정을 반영했다. 후속으로 벌목장 터 직후 건축가를 먼저 배정하는 순서로 보정하고 시나리오 버전을 7로 올렸다.
- [x] **저장 마이그레이션**: 기존 방랑 채집꾼은 가까운 완공 거점의 빈 슬롯에 자동 배정하고, 불가능하면 무직 전환 + 중요 로그로 알린다.
- [x] **guides**: 채광장 초회 도움말을 촌 단계·배정 영역·지정 하역 규칙으로 갱신했다.

## 5. 숙식 움막 세부 (사용자 구술 기준)

### 5-0. 선행 조건 논점 — 전역 소비 대 국지 소비 (2026-08-01 사용자 제기)

현행 소비는 전역이다: 식량·땔감이 마을 곳간(`resources`)에서 일괄 차감되고, 가구(집) 단위 비축은 어디에도 없다. "가져가서 숙식하다 떨어지면 귀가"가 성립하려면 소비의 국지화가 필요하다는 지적은 옳다. 두 길이 있다:

- **A안 (권고): 움막 국지 재고를 특례로 먼저.** 출근 적재 시 전역 곳간에서 차감해 움막 재고로 옮기고, 체류자의 일일 소비는 움막 재고에서만 나간다. 마을 쪽 소비는 전역 유지. 가구당 분배 없이 움막 루프가 완결되고, 침습 범위가 움막 하나로 갇힌다.
- **B안: 가구당 식량·땔감 분배를 먼저 실장.** 모든 집이 비축을 갖고 주민이 제 집 몫을 소비하는 전면 개편 — 온돌 난방의 현실화(집마다 장작), 배급 정책, "전역 냉장고" 제거라는 독립 가치가 크지만, 소비·물류·성능·밸런스 전반을 건드리는 별도 대형 설계다.

**A안으로 확정·구현했다.** 움막(G4)을 가구 분배에 종속시키지 않고, 기존 건물 재고 문법으로 움막 체류자의 식량·땔감만 국지 소비한다. A안의 움막 국지 재고는 나중에 B안이 오면 "가구 비축의 한 사례"로 자연 흡수할 수 있다. **가구당 분배 자체는 독립 설계 후보로 유지**하되(→ §7-6), 본 설계의 선행 조건으로 삼지 않는다.

- 링크: 움막은 채집 건물을 지정해 링크(또는 영역 안 배치 시 자동 링크). 1작업장 1움막부터 시작.
- 보급 루프: 출근 시 마을 곳간에서 **3일분** 식량·땔감을 지고 가 움막에 적재 → 체류하며 일·숙식(일일 소모) → 다음 하루치가 남지 않으면 귀가해 **1일 휴식** → 그 다음 날 재보급 후 복귀. "밤늦게 들어와 금방 나가는" 왕복이 "3일 체류 + 1일 귀가"로 바뀐다.
- 균형·정서 장치: 첫 판은 **3일 체류 상한 + 1일 귀가 휴식** 자체를 제한으로 확정했다. 별도 사기 저하는 중복 벌점이 될 수 있어 넣지 않았다. 병자·격리자·건강 저하자는 움막에 머물지 않는다.
- 위험 노출: 움막 체류자는 마을 밖에서 밤을 보낸다 — 맹수·습격 위협과의 상호작용은 방어 개편 트랙과 접점(후속 결정 4). 첫 판에서는 판정 없이 서사만.

## 6. 경계

- **건설 예정지와 작업영역 충돌**: 신축·영역 확장·이전 목적지의 승인된 개간은 작업영역보다 우선한다. 공사터 나무는 일반 벌목 목표에서 예약 제외하여 개간조 외 벌목꾼과 중복 작업하지 않는다.
- **영역 미지정/영역 내 자원 고갈**: 작업자가 멈추면 반드시 원인을 말해야 한다(기존 "대기 사유" 문법) — "영역에 벨 나무가 없습니다" 류.
- **서식지 고갈로 초보자 굶김**: 사냥은 두 번째 곳간 — 회복 기본값을 후하게, 저장량 UI(G2) 필수.
- **움막 만능화**: 3일분 보급 상한과 의무 1일 귀가로 "전원 움막 이주" 퇴화를 막는다. 작업 슬롯이 있는 연결 거점의 작업자만 이용한다.

## 7. 후속 결정 항목

1. ~~G3 시작 마찰 해소 방식~~ — **벌목장 건설비 인하**로 확정·완료(목재 3, 2일). 시작 제공은 하지 않는다.
2. ~~영역 모양~~ — **원형 반경**으로 확정·완료. 채광장 문법과 통일하고 중심 이동·반경 조절을 제공한다.
3. ~~움막 체류의 사기·일수 상한 수치~~ — **3일 체류 + 1일 귀가, 별도 사기 저하 없음**으로 확정·완료.
4. 움막 체류자의 야간 위험 판정 (방어 개편 P3 이후 재검토)
5. 어부(fisher)도 영역 체제에 편입할지 — 첫 판 제외
6. **가구당 식량·땔감 분배** (§5-0 B안) — 독립 설계 후보. 온돌 난방 현실화·배급 정책·전역 소비 제거와 묶이는 대형 개편. 움막 특례(A안)가 자리 잡은 뒤 별도 설계서로 검토
