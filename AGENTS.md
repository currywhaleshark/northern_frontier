# Northern 저장소 작업 규칙

## 계획 문서 상태 관리

- `docs/` 아래에 설계서, 구현계획, 로드맵 등 앞으로 할 일을 정의하는 문서를 만들 때에는 첫 번째
  제목 바로 아래에 다음 두 줄을 반드시 둔다.

  ```md
  > **계획 상태:** 미착수
  > **상태 갱신:** YYYY-MM-DD — 현재 상태를 한 문장으로 설명
  ```

- 계획 상태는 `미착수`, `일부 완료`, `완료`, `보류`, `대체됨` 중 하나만 사용한다.
  - `미착수`: 설계·계획만 있고 실제 구현 변경이 시작되지 않음.
  - `일부 완료`: 구현이 시작됐거나 일부 범위만 반영되어 남은 작업이 있음.
  - `완료`: 문서에서 확정한 범위가 코드·자산·필요한 검증까지 반영됨. 명시적으로 비범위나
    후속 계획으로 분리한 항목은 완료 판정을 막지 않는다.
  - `보류`: 현재 구현 의사가 없으며 재개 조건이 정해져 있거나 결정 대기 중임.
  - `대체됨`: 다른 문서나 구현 방향이 이 계획을 대신함. 대체 문서 링크를 상태 설명에 남긴다.

- 계획에 따른 구현을 시작하거나 범위를 변경하거나 한 단계를 마칠 때마다, 같은 변경 묶음에서
  해당 계획 문서의 `계획 상태`와 `상태 갱신` 설명을 반드시 갱신한다.
- 여러 단계로 나뉜 계획은 완료된 범위와 남은 범위를 본문 체크리스트나 구현 기록에도 반영한다.
  상단 상태만 바꾸고 오래된 `미착수`, `후속 범위`, 미체크 항목을 그대로 두어 서로 모순되게
  만들지 않는다.
- 계획을 추가하거나 상태를 바꾸면 중앙 색인인 `docs/PLAN-STATUS.md`도 같은 변경 묶음에서
  갱신한다.
- 구현 커밋, 병합, 인수인계가 끝났더라도 위 상태 기록이 갱신되지 않았다면 해당 계획 작업은
  문서상 완료되지 않은 것으로 취급한다.
- 완료 판정에는 가능하면 대표 커밋, 구현 파일, 표적 테스트, 최종 감사 문서 중 하나 이상의
  근거를 상태 설명 또는 `docs/PLAN-STATUS.md`에 남긴다.
